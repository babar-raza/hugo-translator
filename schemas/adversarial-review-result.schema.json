{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "adversarial-review-result.schema.json",
  "title": "Adversarial Review Result",
  "description": "Schema for adversarial-review/review-result.json — the gate file required before ADVERSARIAL_REVIEW transitions to TERMINATED or REWORK_PENDING.",
  "type": "object",
  "required": ["review_date", "challenges", "final_decision", "reason"],
  "properties": {
    "review_date": {
      "type": "string",
      "description": "ISO-8601 date of the adversarial review (YYYY-MM-DD or full datetime)"
    },
    "challenges": {
      "type": "array",
      "description": "List of challenges raised by the adversarial reviewer",
      "items": {
        "type": "string"
      }
    },
    "final_decision": {
      "type": "string",
      "enum": ["ACCEPTED", "REROUTED"],
      "description": "ACCEPTED advances to TERMINATED; REROUTED returns to REWORK_PENDING"
    },
    "reason": {
      "type": "string",
      "description": "Human-readable rationale for the final decision"
    }
  },
  "additionalProperties": true
}
