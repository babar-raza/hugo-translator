-r base.txt

# GPU-optimized dependencies
# Note: faiss-gpu is only available via conda on Windows
# For Windows GPU mode, we use faiss-cpu (PyTorch will still use CUDA)
# On Linux, faiss-gpu provides GPU acceleration for FAISS operations
# faiss-gpu>=1.7.0  # Linux only - install via conda on Windows if needed

ctranslate2>=3.20.0

# Note: Ensure you have CUDA installed
# PyTorch with CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121
